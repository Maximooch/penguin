# September 24, 2026 — Conversation recall and memory

1. **Archive search:** Let Penguin find and read prior sessions under the configured workspace's `conversations/` directory (here: `/Users/maximusputnam/penguin_workspace/conversations`) without switching or changing the active session. Provide `conversation_search` and `conversation_open` for routine, bounded lookup **and** scoped read access to the archive for flexible research with existing file tools or Python. Reuse `context/tasks/conversation-archive-search-plan.md`; avoid reviving the disabled legacy memory stack or adding dependencies for basic lookup. Arbitrary Python/shell execution is not inherently read-only: preserve command permissions and, if offering a read-only mode, enforce it with filesystem isolation rather than a prompt rule; keep transcripts private and exclude unrelated workspace secrets.

2. **Memory direction:** Study `reference/hermes-agent` (especially `tools/memory_tool.py`, `tools/memory_tool_store.py`, and `plugins/memory/`) for what to retain across sessions, when to recall it, user control, and storage boundaries. Compare with Penguin's current notes and provider code; propose the smallest useful design, with archive transcripts as the source of truth and semantic memory optional. Do not adopt Hermes dependencies by default.

**This PR:** Ship read-only archive search/open and archive-only inspection with existing file/Python tools under their current command permissions; no new sandbox or broader write grants. Keep the archive private and avoid exposing other workspace data.

**Follow-up PR stack (later this week):** Harden configurable filesystem/command permission scopes, enforce archive write protection at the process boundary, and specify how scopes propagate to subagents. Until then, do not describe Python/shell access as read-only execution.

**Approach:** Apply Ponytail's reuse-first ladder: check existing capabilities, then stdlib/native options before dependencies; never trade away permissions, privacy, or reliable results for fewer lines.

**Done when:** Penguin can retrieve a specific older exchange with its session reference, and there is a short, evidence-based recommendation for a memory approach before implementing one.

**Archive result / memory recommendation:** Search/open located a June 7, 2025 exchange in `session_20250607_214034_db4f42e9` (user message 1), without loading it as the active session. Hermes keeps small, curated agent/user entries in separate files, freezes their prompt snapshot per session, and gates edits; Penguin already has YAML declarative notes, but not that deliberate recall/edit lifecycle. Keep transcripts canonical; first improve explicit, user-editable notes and selective session-start recall using existing storage. Defer embeddings and Hermes's optional memory providers until deterministic archive lookup and note controls prove insufficient. This PR does not implement semantic memory.
